name: Trigger via Repository Dispatch

on:
  workflow_dispatch:  # Manual trigger
  repository_dispatch:  # Trigger via HTTP request
    types: [manual-trigger]

jobs:
  vpn-job:
    runs-on: ubuntu-latest

    steps:
      - name: Debug Step
        run: echo "Workflow triggered via Power Automate!"
